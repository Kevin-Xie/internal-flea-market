pipeline {
    agent any

    stages {
        stage('Build DOM image') {
            steps {
                sh 'chmod +x ./server/jenkins/build.sh'
                sh './server/jenkins/build.sh'
            }
        }
    }
}